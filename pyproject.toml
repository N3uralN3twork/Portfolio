[project]
name = "portfolio-tooling"
version = "0.1.0"
description = "Python tooling dependencies for the portfolio workspace."
requires-python = ">=3.10"
dependencies = [
    "semble[mcp]",
    "ty>=0.0.34",
]

[tool.uv]
package = false
